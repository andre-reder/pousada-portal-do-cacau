name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Yarn
        run: corepack enable

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Download Decap CMS bundle
        run: bash scripts/setup-admin.sh

      - name: Build with Astro
        run: yarn build
        env:
          # GitHub Pages serves at https://<user>.github.io/<repo-name>/
          # The repo is named "pousada-portal-do-cacau" so base must match.
          # Local dev uses /pousada-portal-do-cacau-9eb6c0 (the folder name).
          BASE: /pousada-portal-do-cacau
          SITE: https://andre-reder.github.io

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
